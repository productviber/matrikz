{
  "name": "matrikz",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "typecheck": "npm run --workspaces typecheck",
    "test": "npm run --workspaces test",
    "verify": "npm run typecheck && npm test"
  }
}
